const logo = ['tardis_draw', 'cyberman', 'dalek'];
const randomLogo = document.querySelector('.js-random-logo');

if (randomEmoji) {
  randomLogo.textContent = emoji[Math.floor(Math.random() * emoji.length)]
}
